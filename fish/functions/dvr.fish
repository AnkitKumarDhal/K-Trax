function dvr --wraps='set -x RUSTICL_ENABLE iris && /opt/resolve/bin/resolve' --description 'alias dvr=set -x RUSTICL_ENABLE iris && /opt/resolve/bin/resolve'
  set -x RUSTICL_ENABLE iris && /opt/resolve/bin/resolve $argv
        
end
